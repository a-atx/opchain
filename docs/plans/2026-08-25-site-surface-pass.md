# opchain.dev surface pass — change / update / overhaul

_2026-08-25. `/oc-app-architect` (content accuracy vs the post-Apache-2.0, pre-split reality) + `/oc-ux-engineer` (Design Evaluator) two-lens pass over all 23 routes + shared components. Five bucket reviewers + a live-drift check of prod (still serving off-main `c14936d`), synthesized adversarially with 18 source spot-checks; one synthesizer error (the "dead private-repo links" premise) caught and corrected — see Spot-check corrections. Companion to [2026-08-22-oss-split-licensing-compliance.md](2026-08-22-oss-split-licensing-compliance.md); wave labels align with its §5._

Adversarial synthesis of five reviewer buckets. **18 spot-checks run against source** (privacy.astro, src/index.js notify/votes/mcp/LICENSE handlers, security.astro ×3 regions, status.astro, Footer.astro, security.txt + `.well-known/` listing, index.astro stats, compare.astro:88+footnote, install.astro:113/417, WelcomePopup, SearchPalette:36-52/410, CaptureModal:27/316, glossary ×5, coverage:53, architecture:32/1317-1327, roles.ts, content.config.ts, showcase:100-117 + dashboard-static.ts, changelog:222 + card census, roadmap-static importer grep, dashboard.astro styles + tokens.css grep, discovery.js, build-site.sh, Base.astro, walkthroughs/index.ts). Nearly everything reproduces. Three corrections:

## Spot-check corrections (applied below)

1. **`LEAD_TTL_DAYS` "appears nowhere in the codebase" — overstated.** It appears once, as a dead optional type in `site/src/env.d.ts:11`. Functionally the finding stands: the Worker never reads it and `env.NOTIFY.put()` has no `expirationTtl` (src/index.js:424) — leads (raw email + IP + UA) persist indefinitely. Add: delete the stale env.d.ts declaration when rewriting /privacy.
2. **Changelog card count corrected.** Reviewer claimed "15 cards (5 heroes + 10 rel-cards)". Actual released-panel census: **6 hero-cards + 10 rel-cards = 16**. The finding survives either way — the hardcoded "13 shipped" (changelog.astro:222) matches nothing.
3. **D1's premise was false** (caught after synthesis): `github.com/asfbay-bit/opchain` is public and returns 200 unauthenticated — no link on the site is dead. Every "(D1)" item below is re-framed as centralize-now / retarget-at-split.
4. **Confirmed-fixed items stay fixed** (no one should re-fix): status.astro's "counts PRs" JS is gone (plain link now); security.astro:263, compare cost cells, and uses license row already say Apache-2.0; Footer © line + `/LICENSE` link correct; build-site.sh:55-57 + src/index.js:804-815 ship and content-type-pin /LICENSE + /NOTICE; discovery.js license fields at :99-101/:118/:162/:183-184 verified.

Everything else cited in this doc was verified or is consistent with verified neighboring lines.

## Deduplicated cross-cutting fixes (do once, not per page)

- **D1 — Repo links hardcoded across 9+ surfaces** (CORRECTED — the synthesizer claimed these 404; verified 2026-08-25: `github.com/asfbay-bit/opchain` returns **200 unauthenticated**, the monorepo is public and D3 keeps it that way, so nothing is broken today): Footer.astro:45, index.astro stat tile (~118), glossary.astro:168, coverage.astro:53 (`REPO_TREE` — feeds ~8 links), status.astro:65/:85, security.astro:11/:158, security.txt:5, uses.astro:89, architecture.astro:1385. The real issue is the **split**: once `opchain-skills` is the contributor repo, every "GitHub / open an issue / source" link points at the wrong home. Fix **now** (S): centralize into one exported const (`site/src/lib/repo.ts`) so the split becomes a one-line flip; retarget issue/contribute links to `opchain-skills` **at the split**.
- **D2 — Release/count string drift (~30 strings).** "v1.8" ×3 (Header.astro:16, index stat, index release bar), "29 skills" (~20 sites incl. install:113, architecture ×3, MobileArchitecture ×6), "22 skills" (compare:88), "24 skills" (architecture:32 meta), "18 checkpoints" ×2 + "five tri-agent" + "eight stages" (glossary), "Tri-Agent (7)" vs 8 (architecture:1317 + MobileArchitecture:873 — oc-signal-forge missing from both), "six sessions" ×2 (WelcomePopup:34-46, install:417 — actual: 12 walkthroughs, verified), "13 shipped" (changelog:222 — actual 16), "v1.8.0 latest" hero badge (actual v1.8.2). Fix: one release/counts module + one release-pinned-surfaces test asserting against `getAllSkills()`/frontmatter.
- **D3 — License plumbing 90% done, 0% visible.** All SKILL.md carry `license: Apache-2.0` but content.config.ts (verified :43-57) strips the field; no `license` in any JSON-LD (Base.astro:104-120 verified, index softwareLd, skills/[id] skillLd); no license microcopy at any download CTA (/install, /skills, /skills/[id], pipeline-builder). Fix: one schema line + badge + JSON-LD key + `<link rel="license">` + CTA microcopy.
- **D4 — Server-side capture vs "consent-gated" claims.** PostHog server events are env-gated, not consent-gated (src/index.js:334/:432/:790, verified pattern at notify). Contradicted on privacy.astro:46-51 and security.astro:259. Fix wording in both.
- **D5 — Missing `aria-live` on every form/status region:** security.astro #sec-status, status.astro overall-text, CaptureModal, FeedbackWidget, showcase #sc-status. One pattern, five applications.
- **D6 — Token debt, recurring shapes:** undefined `--dim` (verified absent from tokens.css; used in CaptureModal:316, showcase, glossary, pipeline-builder, security); hardcoded `#1c1710` for on-accent text (= `--obsidian`, 4+ files); dead/wrong hex fallbacks (`var(--success,#0D9488)` etc. — three different "danger" reds across the site); AA ember pair `#f5894d/#993505` copy-pasted ×4; dashboard.astro's entire foreign vocabulary (`--text-2/--text-3/--surface-2`, verified nonexistent → slate fallbacks render, light theme ~unreadable).
- **D7 — security.txt single-sourcing:** hand-duplicated in security.astro:7-11 (verified identical) and both copies lack RFC 9116-required `Expires`.

## Surface table

Wave: **now** = before/with the next deploy · **with-split** = lands with the repo inversion · **later**. The deploy itself (main + #422) is row 1 — half the "live" bucket resolves for free.

| Route / surface | Verdict | Wave | Justifying items | Effort |
|---|---|---|---|---|
| **Deploy main+#422 to staging→prod** | — | now | Prod serves off-main `c14936d` (~1 month stale, not on origin/main); fixes live MIT→Apache ×4, serves /LICENSE+/NOTICE, license fields in skills.json/llms.txt/ai-catalog/mcp.json, zips rebuilt with LICENSE+NOTICE. Verify with cache-busting (1h edge cache on zip/discovery); close deploy-lag issue | S |
| `/privacy` | **overhaul** | now | Leads with removed Try-It feature + fabricated 365-day TTL (real: no TTL, raw email+IP+UA — verified); "1 hour" counters (real: 60s, /api/notify only); omits votes store, MCP checkpoints (no TTL — disclose as-is, don't overclaim planned fixes), `oc_id` cookie, server-side capture (D4); fork framing wrong (Worker stays private). Add last-updated stamp; delete env.d.ts:11 stale type | M |
| `/security` + security.txt | **change** | now | Scope omits `/mcp` (highest-risk surface) + `/api/votes` — safe harbor doesn't cover them; `Expires` missing (D7, RFC MUST); advisories links work (PVR is now enabled) but the advisories home per repo needs deciding at the split (D1); "consent-gated" (D4); KV row omits votes/MCP data. UX: error color semantics, aria-live (D5) | M |
| `/status` | update | now | GitHub compare/issues links via the shared const — issues move to opchain-skills at the split (D1); "every edge-served route is reachable" overclaim; "historical uptime" meta with no history. UX: aria-live, refresh-button race | S |
| `/` (index) | update | now | "Apache-2.0 / open source" stat tile should route through the shared repo const (D1 — link works today, flips at split); v1.8 strings (D2); softwareLd license (D3); WCAG 2.2.2 auto-rotating banner with no stop control. With-split: contribute card | M |
| `/install` | update | now | "29 skills" literal (D2); "six sessions, end to end… end to end" (real: 12); version-check blocks point at build SHA not `catalogVersion`; "don't phone home" section needs one-line beacon disclosure before privacy rewrite lands; zero license mention at download CTAs (D3) | M |
| `/demo` + WelcomePopup | update | now | Popup "six finished sessions" + hand-enumerated six blurbs contradict page's own "twelve scenarios" meta (both verified). Otherwise honest post-Try-It (no email gate — true). UX: popup needs native `<dialog>`/focus trap | S |
| SearchPalette | update | now | Fabricated `npx opchain install` copy action (verified :410) — npm name unclaimed = squat-training hazard; **new finding:** the Install row blurb "npm path, npx variant, manual bundle" (verified :51) fabricates the same nonexistent channels; pages list missing /ai-recipes, /for-agencies, /blog, /dashboard (verified); broken listbox ARIA | S |
| CaptureModal + FeedbackWidget | update | now | "Send me when v2 ships?" (no v2 exists — verified); no privacy link on an email+role+teamSize form whose backend has no TTL; undefined `--dim` (D6); no focus trap; aria-live (D5) | S |
| Footer | update | now | GitHub link via the shared const, retarget at split (D1); "unsubscribe anytime" with no unsubscribe mechanism; token fixes (`--radius-sm`, `--accent-contrast` don't exist). © line + /LICENSE verified correct — don't touch | S |
| `/skills` | update | now | "v1.8 · SHIPPED" callout (D2); lede "one skill per phase" false; license microcopy at zip button (D3) | S |
| `/skills/[id]` | **change** | now | Schema strips `license` frontmatter so page can't render it (verified content.config.ts); skillLd missing license (D3); no source/contribute link (mirror serves the path today); "Updated" badge dies at split (see skills.ts row) | M |
| `/architecture` + MobileArchitecture | update | now | Meta "24 skills" vs page "29" (both verified); Tri-Agent "(7)" vs 8, oc-signal-forge missing (verified, both files); ".checkpoints/<skill>.json" wrong filename ×2; "chains-tod" typo ×3; hardcoded repo slug via shared const (D1); desktop/mobile theme philosophy splits at 768px | M |
| `/glossary` | update | now | "Open an issue" should target the contributor repo once PRs merge there (D1); "18 checkpoints" ×2, "Five skills" tri-agent (real: 8), "eight stages" vs 9-stage Skillchain vs real 4-phase frontmatter (all verified); missing Pack/Plugin terms; DCO term at split | S |
| `/coverage` | update | now | `REPO_TREE` hardcodes the monorepo (~10 links, working today) — route through the shared const so the split is a one-line flip (D1); "No client-side JS" comment drift; gen-script named as if public | S |
| `/pipeline-builder` | **change** | now | Install snippet omits the recommended plugin path (which ships the enforcement hooks); decision table predates v1.7/v1.8 (no oc-fleet-ops for self-hosted k8s, no docs-forge/repo-ops anywhere); "/orchestrator" isn't a registered command; invisible keyboard focus on the wizard's radio cards (WCAG 2.4.7) | M |
| `/compare` | update | now | "22 skills" in hero column (verified); "refreshed 2026-06-24" stamp false (cells edited 2026-08-24, verified Apache-2.0 cells present); optional License row post-split | S |
| `/changelog` | update | now | **Missing the Apache-2.0 relicense + LICENSE/NOTICE-in-artifacts entry entirely** — the one change every consumer is legally affected by; "13 shipped" vs 16 rendered cards (corrected census); hero badge "v1.8.0 latest" vs v1.8.2; reconcile v1.9 vote framing at split | M |
| `/showcase` | update | now | **Top overclaim on the site:** "$7.41 measured… not estimated… never a guess" is dashboard-static.ts:46 with `sample: true` (both verified) — fabricated data presented as measured; gallery stale (one self-entry since Feb); `--dim`, error-color semantics, aria-live (D5/D6) | S |
| `/dashboard` | update | now | Honestly sample-labeled (verified banner) but "telemetry is rolling out" promise 2 months stale (installed in zero repos per memory); stylesheet never adopted tokens.css — foreign vars render slate fallbacks, light theme ~1.3:1 unreadable (verified D6) | S |
| Base.astro | update | now | No `license` / `sameAs` in site JSON-LD (verified), no `<link rel=license>`; twitter:card `summary` despite 1200px OG cards (verified); no RSS autodiscovery; no skip-to-content link. sameAs → with-split | S |
| Header | update | now / with-split | Third independent "v1.8" (D2, now); menu-role ARIA without arrow keys; GitHub/Contribute nav entry at split | S |
| `/uses` | update | now | "Last refresh: 2026-05"; repo row via the shared const (D1); KV row omits votes/MCP; no MCP/discovery rows on the how-it's-built page; link the License row to /LICENSE | S |
| `/.well-known/did.json` | **change** | now | Advertised by ai-catalog (discovery.js:84, verified) but file absent from repo (verified — `.well-known/` holds only security.txt); deploy doesn't fix. Mint locally via gen-did.mjs + commit, **or drop the identifier** — advertising an unresolvable DID is worse than none | S |
| skills.ts / roles.ts | **change** | now (map) / with-split (dates) | ROLE_BY_NAME has 22/29 entries (verified) — signal-forge/docs-forge/repo-ops/fleet-ops/modularize-ops/cost-ops/telemetry-ops all fall back to "specialist"; add build-time completeness check. Git-derived "Updated" dates break at history inversion — stamp per-skill dates into a generated artifact **before** the split (seam-PR prerequisite) | M |
| roadmap-static.ts | **change** (delete) | with-split | Dead code — zero importers (verified grep); self-documents as canonical while changelog hardcodes a divergent copy. Delete or wire | S |
| ConsentBanner | update | later | Banner behavior verified honest; role=dialog semantics without dialog behavior; credibility inherits from /privacy fix | S |
| `/styleguide` | update | later | Missing role tokens + in-progress/destructive swatches; type labels off by desktop +1px bump; hardcoded "v1.8.2" badge sample | S |
| `/for-agencies` | keep | later | Stack-choice attribution blurs oc-stack-forge/oc-ux-engineer boundary; optional Apache-2.0 deliverables line; dead `counter-reset` CSS | S |
| `/ai-recipes` | keep | later | Commands verified real; add 5-line build assert that step skills exist | S |
| `/blog` + `/blog/[slug]` + rss.xml | keep | later | Templates healthy (rss.xml.ts verified clean, data-driven); gap is editorial — no post since 2026-07-10; relicense/OSS-split post is the strongest untold story, land at split | S |
| PipelineDiagram.astro | **change** (delete/park) | later | Unrendered; "kept in sync" comment false (~15-skill-era snapshot); park under design/ or delete | S |
| `/404` | keep | — | Verified clean; no changes | — |

## Top 5 highest-leverage actions

1. **Ship the deploy** (staging from pulled main → eyeball → prod). One manual action erases the largest live-truth deficit: 4 MIT claims, missing /LICENSE + /NOTICE, license-less discovery surfaces, license-less zips, month-stale off-main SHA. Everything else in "now" should ride in main before or immediately after.
2. **`site/src/lib/repo.ts` — one shared repo const, adopted by all 9+ hardcoded links now** (D1, corrected). The links work today (repo is public); the point is that the split then becomes a one-line flip instead of a 9-file sweep, and issue/contribute links can move to `opchain-skills` the day PRs merge there.
3. **Rewrite /privacy + /security scope + security.txt Expires as one compliance PR** (D4, D7). The privacy page describes a removed product and a fabricated TTL while omitting three real data stores — the worst possible failure mode for that page type, and it blocks honest framing of the install page's update-check beacon.
4. **One release/counts module + release-pinned-surfaces test** (D2). Retires ~30 hand-maintained strings across 10 files in one pass; the MEMORY.md checklist already exists — turn it into an assertion so v1.9 can't recreate this audit.
5. **Kill the fabrications:** SearchPalette's `npx opchain install` + "npm path" blurb (unclaimed npm name = active squat hazard), showcase's "$7.41 measured, never a guess" over `sample: true` data, CaptureModal's "when v2 ships", pipeline-builder's `/orchestrator`. Four small edits; each is a trust wound where a visitor can catch the site lying.

## Pages wrongly spared / coverage gaps

- **/dashboard — real, not placeholder** (verified: full page rendering dashboard-static.ts with an honest `sample: true` banner). Reviewers' "update" stands, but the broken light theme (foreign token vocabulary, verified) makes it functionally unshippable in light mode — treat the token remap as non-optional within "now".
- **/demo — exists and is honest post-Try-It** ("no live chat, no email gate, no install required" all verified true). Only defect is the six-vs-twelve contradiction, already captured.
- **SearchPalette Install blurb** ("npm path, npx variant, manual bundle", :51) — fabricated install channels adjacent to the flagged :410; no reviewer caught it. Added above.
- **`site/src/env.d.ts:11`** — orphaned `LEAD_TTL_DAYS` type nobody flagged; delete with the privacy rewrite.
- **/blog/rss.xml.ts** — reviewed by nobody; spot-checked clean (data-driven, escaped, no stale claims). Keep.
- **/404, /for-agencies, /ai-recipes, ConsentBanner** — spared correctly; spot-checks support the keep/later verdicts.
- **Synthesizer error, corrected:** the "private repo / dead links" premise behind D1 was false — `asfbay-bit/opchain` is public (200 unauthenticated, checked 2026-08-25) per decision D3. Centralizing the slug is a now item; *retargeting* is a with-split item.